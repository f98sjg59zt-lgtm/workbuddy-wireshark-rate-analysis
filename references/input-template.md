# 用户输入模板

分析开始前，用户需按以下格式提交故障信息。**缺失项标注「未知」，不得臆测填补。**

```markdown
## 故障描述

### 1. 环境拓扑
[A端 IP/主机名] ←→ [链路类型/带宽] ←→ [B端 IP/主机名]
可选：经 [中间设备]

### 2. 传输方向与抓包点（关键！）
数据流向：从 [IP-A] 发往 [IP-B]
抓包点位置：[A端（发送端） / B端（接收端） / 中间镜像端口 / 不确定]

> ⚠️ 抓包点直接影响 BIF 可信度、RTT 解读方向。若不确定，Skill 将通过 TTL 自动推断（非 100% 准确）。
```
